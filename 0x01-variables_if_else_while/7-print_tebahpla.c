#include <stdio.h>

/**
 * main - Prints numbers from 1 to 9 .
 * 
 * Return: Always 0.
 */
int main(void)
{
    char ch;
    for (ch = 'z'; ch >= 'a'; ch--)
        putchar(ch);
		
	putchar('\n');

	return (0);
}

