#include "main.h"

/**
 * main - Print 10 times the alphabet, in lowercase, followed by a new line.
 * Return: Always 0
 */
int _isalpha(int c)
{
     if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	     reurn (1);
	else
		return (0);
}
